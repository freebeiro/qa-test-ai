// Main background script - import modules
import { handleCommand } from './background-core.js';

// Initialize and re-export
console.log('Background script initialized');

// Export the handleCommand for testing purposes
export { handleCommand };
